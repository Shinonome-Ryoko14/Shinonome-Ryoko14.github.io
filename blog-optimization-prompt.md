# 个人 Blog 项目重构与优化需求文档

## 项目背景

这是一个个人 Blog 项目，当前基于旧有技术栈构建，存在配置持久化缺失、功能冗余、安全性不足以及扩展性差等问题。本文档描述全面重构与优化的目标与约束。

---

## 一、核心问题修复

### 1.1 配置持久化（优先级：高）

**问题描述：**
管理员在后台修改特效、公告、密码等配置项后，换一台设备打开博客，所有配置不生效，恢复为默认值。

**期望行为：**
- 所有配置项（特效参数、公告内容、管理员密码哈希等）必须持久化存储至服务端数据库或配置文件
- 前端启动时从服务端拉取配置，而非使用硬编码默认值或仅存储于 `localStorage`
- 配置变更后立即同步写入持久化层，多端访问保持一致

**技术要求：**
- 后端提供 `/api/config` 相关 RESTful 接口（GET 读取、PATCH 更新）
- 配置项需区分「公开配置」（前端可直接读取）与「私有配置」（需鉴权才能修改）

---

### 1.2 功能瘦身：移除多用户功能（优先级：高）

**问题描述：**
博客定位为个人使用，当前存在用户注册、用户发帖等多用户功能，属于冗余模块。

**期望行为：**
- 完全删除用户注册、登录（访客）、用户发帖、用户评论等相关页面、路由、API 与数据库表
- 保留管理员单一账号体系
- 访客只具有只读权限（浏览文章、查看公告）

---

## 二、安全性改造

### 2.1 管理入口隐藏（优先级：高）

**问题描述：**
管理员入口暴露在主页，存在被暴力破解或扫描的风险。

**期望行为：**
- 主页不显示任何管理员入口链接或按钮
- 通过**隐藏路由 + 密码验证**的方式进入管理界面，例如：
  - 访问特定隐藏路径（如 `/manage-xxxxxx`，路径可在配置中自定义）
  - 输入管理员密码后方可进入后台
- 管理员密码以哈希（bcrypt / argon2）形式存储，禁止明文保存
- 支持 JWT 或 Session 鉴权，Token 设置合理过期时间

---

## 三、技术栈升级与重构

### 3.1 现代化技术栈（优先级：高）

使用以下技术栈对项目进行全面重构，提升健壮性与可维护性：

| 层级 | 推荐技术 | 说明 |
|------|---------|------|
| 语言 | **TypeScript** | 全栈启用严格类型检查 |
| 前端框架 | **React 18 / Next.js 14+** 或 **Vue 3 + Vite** | 按现有技术栈偏好选择 |
| 状态管理 | **Zustand** / **Pinia** | 轻量、类型友好 |
| 后端 | **Node.js + Express / Fastify** 或 **Next.js API Routes** | RESTful API |
| 数据库 | **SQLite**（单机）或 **PostgreSQL**（扩展） | ORM 使用 **Prisma** |
| 样式 | **Tailwind CSS** + CSS Modules | 兼顾效率与隔离 |
| 构建工具 | **Vite** 或 **Turbopack** | 快速热更新 |
| 代码质量 | **ESLint + Prettier + Husky** | 统一规范 |

**重构要求：**
- 启用 TypeScript 严格模式（`"strict": true`）
- 所有 API 请求/响应定义共享类型（可使用 `zod` 做运行时校验）
- 组件拆分合理，避免单文件超过 300 行
- 编写必要的单元测试（核心工具函数、API handler）

---

## 四、文章编辑器

### 4.1 富文本 / Markdown + LaTeX 支持（优先级：中）

**期望功能：**
- 编辑器支持 **Markdown** 语法，实时预览
- 支持 **LaTeX 数学公式**（行内 `$...$` 与块级 `$$...$$`），渲染使用 **KaTeX** 或 **MathJax**
- 支持代码块语法高亮（**highlight.js** 或 **Shiki**）
- 支持图片上传与插入（本地上传 / URL 引用）
- 编辑器推荐使用 **@uiw/react-md-editor** 或 **Milkdown** 或 **CodeMirror 6 + unified/remark**

**文章数据结构（示例）：**
```typescript
interface Article {
  id: string;
  title: string;
  content: string;        // 原始 Markdown 源码
  contentHtml?: string;   // 预渲染 HTML（可选缓存）
  tags: string[];
  createdAt: Date;
  updatedAt: Date;
  published: boolean;
}
```

---

## 五、特效系统扩展

### 5.1 约束说明（优先级：高）

> ⚠️ **严格保护现有特效，尤其是重力系统和泛光效果。**
> - 现有特效代码只允许做**局部性能优化**（如减少不必要重绘、requestAnimationFrame 节流），**不得改变视觉输出**
> - 新增特效必须以**独立模块**形式注册，不得侵入现有特效的渲染管线
> - 建议将特效系统抽象为插件架构（Effect Plugin System），每个特效为一个独立的 class 或 composable

### 5.2 新增可视化特效面板

在管理后台增加**特效配置面板**，支持以下特效的开关与参数调节：

| 特效名称 | 说明 |
|---------|------|
| **手写动效** | 页面元素以手写笔触动画形式出现（SVG path 动画） |
| **照片破碎** | 图片点击或切换时产生碎片化离散动画（Canvas / WebGL） |
| **转页动画** | 页面跳转时的翻页过渡效果（CSS 3D Transform） |
| **按键 Q 弹动画** | 按下键盘按键时，对应元素产生弹跳/果冻感（Spring 物理动画） |
| **拖拽动画** | 元素支持拖拽，带有惯性跟手与回弹效果 |
| **更换背景** | 支持上传自定义背景图 / 渐变 / 纯色，实时预览 |
| **磨砂玻璃** | 为卡片、侧边栏等组件启用 `backdrop-filter: blur` 效果，可调节模糊强度与透明度 |

**特效面板技术要求：**
- 每个特效可独立**开启/关闭**，配置实时生效并持久化
- 面板提供**参数滑块/颜色选择器/数值输入**等控件
- 特效参数变更后通过 `/api/config` 接口保存至后端
- 前端加载时读取配置，按需初始化对应特效模块

---

## 六、交付要求

- [ ] 提供完整的项目目录结构说明
- [ ] 关键模块附带简要注释
- [ ] 数据库 Schema 定义（Prisma schema 或 SQL DDL）
- [ ] 环境变量模板（`.env.example`）
- [ ] 启动与部署说明（`README.md`）
- [ ] 新旧特效并存的兼容性验证方案

---

## 七、优先级汇总

| 优先级 | 模块 |
|--------|------|
| 🔴 P0（阻塞） | 配置持久化、移除多用户功能、管理入口隐藏 |
| 🟠 P1（核心） | TypeScript 重构、特效系统插件化、保护现有特效 |
| 🟡 P2（重要） | Markdown + LaTeX 编辑器、特效可视化面板 |
| 🟢 P3（增强） | 单元测试、性能优化、部署文档 |
